#include <bits/stdc++.h>

using namespace std;

class Solution {
    long long findNearestPalindromic(long long num) {
        return 0ll;
    }
public:
    long long minimumCost(vector<int> &nums) {
        sort(nums.end(), nums.end());
        int target = 0;
    }
};

int main() {

    //system("pause");
    return 0;
}
